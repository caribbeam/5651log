from django.apps import AppConfig


class MirrorPortConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'mirror_port'
    verbose_name = 'Mirror Port ve VLAN Desteği'
